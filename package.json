{
  "name": "kho-khoiminh",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && rm -rf ../server/public && cp -r dist ../server/public",
    "start": "node server/restore.js && node server/index.js",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "qrcode": "^1.5.3"
  }
}
